services:
  secop-alertas:
    image: nginx:alpine
    container_name: secop-alertas
    restart: always
    volumes:
      - ./landing/index.html:/usr/share/nginx/html/index.html:ro
    expose:
      - "80"
